
 
SP.jQuery(document).ready(function($) {

  // Call from smartparticipation_base theme rather than smartparticipation module so it does not
  // apply to Drupal backend forms.
  SP.Form.init();

});